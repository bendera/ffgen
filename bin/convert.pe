input_file = $argv[1]
target_dir = $argv[2]
BACKSLASH = Chr(92)

Open(input_file, 1)

i = 3
while(i < $argc)
	Generate(target_dir + BACKSLASH + $argv[i])
	i = i + 1
endloop

WriteStringToFile($fontname, target_dir + BACKSLASH + "originalfontname.txt")
